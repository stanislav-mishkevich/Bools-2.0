# Новые компоненты в стиле Logisim

Добавлены следующие категории компонентов для создания полноценных цифровых схем:

## 🔄 Триггеры (Flip-Flops)
- **D-триггер** (`D_FLIPFLOP`) - сохраняет значение D при положительном фронте CLK
  - Входы: D (данные), CLK (тактовый сигнал)
  - Выходы: Q, Q̄ (инверсный)
  
- **T-триггер** (`T_FLIPFLOP`) - переключает состояние при T=1 на фронте CLK
  - Входы: T (toggle), CLK
  - Выходы: Q, Q̄
  
- **JK-триггер** (`JK_FLIPFLOP`) - универсальный триггер
  - Входы: J, CLK, K
  - Выходы: Q, Q̄
  - Логика: J=K=0 (держать), J=1,K=0 (установить), J=0,K=1 (сбросить), J=K=1 (переключить)
  
- **SR-защелка** (`SR_LATCH`) - базовая схема памяти
  - Входы: S (set), R (reset)
  - Выходы: Q, Q̄

## 🔀 Мультиплексоры и демультиплексоры
- **MUX 2:1** - выбирает один из двух входов по селектору
- **MUX 4:1** - выбирает один из четырех входов по двум селекторам
- **DEMUX 1:2** - направляет вход на один из двух выходов
- **DEMUX 1:4** - направляет вход на один из четырех выходов

## 📊 Счетчики и регистры
- **4-битный счетчик** (`COUNTER_4BIT`) - считает тактовые импульсы
  - Входы: CLK, RST
  - Выходы: Q0-Q3 (4-битное значение)
  
- **4-битный регистр** (`REGISTER_4BIT`) - хранит 4 бита данных
  - Входы: D0-D3, CLK, LOAD
  - Выходы: Q0-Q3

## ➕ Сумматоры
- **Полусумматор** (`HALF_ADDER`) - складывает два бита
  - Входы: A, B
  - Выходы: SUM, CARRY
  
- **Полный сумматор** (`FULL_ADDER`) - складывает три бита
  - Входы: A, B, Cin
  - Выходы: SUM, Cout
  
- **4-битный сумматор** (`ADDER_4BIT`) - складывает два 4-битных числа
  - Входы: A0-A3, B0-B3, Cin
  - Выходы: S0-S3, Cout

## 🔢 Декодеры и энкодеры
- **Декодер 2:4** (`DECODER_2TO4`) - преобразует 2-битный код в одну из 4 линий
- **Декодер 3:8** (`DECODER_3TO8`) - преобразует 3-битный код в одну из 8 линий
- **Энкодер 4:2** (`ENCODER_4TO2`) - преобразует 4 линии в 2-битный код (приоритетное кодирование)

## ⚖️ Компараторы
- **1-битный компаратор** (`COMPARATOR_1BIT`) - сравнивает два бита
  - Выходы: A>B, A=B, A<B
  
- **4-битный компаратор** (`COMPARATOR_4BIT`) - сравнивает два 4-битных числа
  - Входы: A0-A3, B0-B3
  - Выходы: A>B, A=B, A<B

## 💾 Память
- **RAM 4×4** (`RAM_4X4`) - оперативная память на 4 слова по 4 бита
  - Адресные входы: A0, A1
  - Входы данных: D0-D3
  - Управление: WE (write enable)
  - Выходы: Q0-Q3
  
- **ROM 4×4** (`ROM_4X4`) - постоянная память на 4 слова по 4 бита
  - Адресные входы: A0, A1
  - Выходы: D0-D3

## ⏰ Генераторы
- **Генератор тактов** (`CLOCK`) - генерирует периодические импульсы
  - Выход: CLK (переключается автоматически)
  - Частота настраивается через свойства компонента

## 🚌 Шины
- **4-битный разветвитель** (`SPLITTER_4BIT`) - разделяет 4-битную шину на отдельные линии
  - Вход: IN (4-битная шина)
  - Выходы: B0-B3 (отдельные биты)
  
- **4-битный объединитель** (`COMBINER_4BIT`) - объединяет отдельные линии в 4-битную шину
  - Входы: B0-B3 (отдельные биты)
  - Выход: OUT (4-битная шина)

## 🎨 Использование

Все новые компоненты доступны в боковой панели, распределены по категориям:
- Триггеры
- Мультиплексоры
- Счетчики и регистры
- Сумматоры
- Декодеры и энкодеры
- Компараторы
- Память
- Генераторы
- Шины

Компоненты имеют:
- ✅ Полную локализацию (русский и английский)
- ✅ Описания и подсказки
- ✅ Правильную логику симуляции
- ✅ Поддержку циклических схем
- ✅ Визуальное отображение состояния

## 💡 Примеры применения

### Создание простого счетчика
1. Добавьте **4-битный счетчик**
2. Подключите **Генератор тактов** к входу CLK
3. Добавьте 4 **OUTPUT** элемента к выходам Q0-Q3
4. Наблюдайте автоматический счет от 0 до 15

### Создание ALU (арифметико-логическое устройство)
1. Используйте **4-битный сумматор** для сложения
2. Добавьте **Мультиплексоры** для выбора операций
3. Используйте **Компаратор** для операций сравнения
4. Подключите **Регистры** для хранения результатов

### Создание памяти с адресацией
1. Добавьте **RAM 4×4**
2. Используйте **INPUT** элементы для адреса (A0, A1)
3. Добавьте **INPUT** для данных (D0-D3)
4. Используйте **SWITCH** для управления записью (WE)
5. Подключите **OUTPUT** к выходам для чтения

## 🔧 Технические детали

### Состояние компонентов
Триггеры и память используют внутреннее состояние (`internalState`, `memoryData`), которое сохраняется между симуляциями.

### Детекция фронта
Триггеры реагируют на положительный фронт CLK (переход 0 → 1) с использованием поля `previousClock`.

### Множественные выходы
Компоненты с несколькими выходами (декодеры, сумматоры) используют прямое управление массивом `outputPins`.

### Поддержка циклов
Все компоненты корректно работают в циклических схемах благодаря итеративной релаксации в симуляторе.
